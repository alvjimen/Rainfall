#include <unistd.h>

int run(void)
{
    char buffer[0x18];

    fwrite("Good... Wait what?\n" 1, 0x13, stdout);
    return (system("/bin/sh"));
}

int main(void)
{
  char buffer[64];

  return (gets(buffer));
}
